apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.service_democrm.name }}
spec:
  selector:
    app: {{ .Values.Global.name }}
  ports:
  - port: {{ .Values.service_democrm.port }}
    targetPort: {{ .Values.deployment.port }}
